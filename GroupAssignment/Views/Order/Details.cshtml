@model GroupAssignment.Models.OrderEntity

@{
    ViewData["Title"] = "Order Details";
}
@* Dulan Wasala *@
@* 991574529 *@
@* Group Assignment *@ 
@* Order Details View *@
@* This view is used to display the details of an order. It includes a table for displaying the products in the order. *@
<style>
    .info-text {
        color: #155724; /* Dark green for better contrast */
    }

    .custom-header {
        background-color: #d4edda; /* Lighter green header background for contrast */
        color: #0c5460; /* Dark blue text color for contrast */
    }

    .custom-body {
        background-color: #f8f9fa; /* Light gray body background, ensuring dark text is visible */
    }

    .table-custom {
        background-color: #ffffff; /* White background for table ensures text visibility */
        border: 1px solid #dee2e6; /* Light gray border for subtle contrast */
    }

    .btn-custom-edit {
        background-color: #28a745; /* Bootstrap green color for edit button */
        color: white; /* White text for contrast */
        margin-right: 5px; /* Add some spacing between the buttons */
    }

    .btn-custom-back {
        background-color: #6c757d; /* Bootstrap gray color for back button */
        color: white; /* White text for contrast */
    }
</style>

<div class="container mt-4">
    <h1 class="info-text">@ViewData["Title"]</h1>

    <div class="card border-info mb-4">
        <div class="card-header custom-header">
            <h4 class="mb-0">Order Details</h4>
        </div>
        <div class="card-body custom-body">
            <dl class="row">
                <dt class="col-sm-4">Username:</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.UserName)</dd>

                <dt class="col-sm-4">Manufacturer Name:</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.MFRName)</dd>

                <dt class="col-sm-4">Order Description:</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.OrderDescription)</dd>

                <dt class="col-sm-4">Order Status:</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.OrderStatus)</dd>

                <dt class="col-sm-4">Order Date:</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.OrderDate)</dd>

                <dt class="col-sm-4">Delivery Date:</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.OrderDeliveryDate)</dd>
            </dl>
        </div>
    </div>

    <div class="card border-info">
        <div class="card-header custom-header">
            <h4 class="mb-0">Products</h4>
        </div>
        <div class="card-body custom-body">
            <table class="table table-striped table-custom">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Product Name</th>
                        <th>Quantity</th>
                        <th>Description</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in Model.Products)
                    {
                        <tr>
                            <td>@product.Id</td>
                            <td>@product.ProductName</td>
                            <td>@product.ProductQuantity</td>
                            <td>@product.ProductDescription</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div class="mt-3">
        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-custom-edit">Edit</a>
        <a asp-action="Index" class="btn btn-custom-back">Back to List</a>
    </div>
</div>